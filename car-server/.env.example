# 本地 RAG 无需任何 API Key。本文件为可选配置（一般不需要）。
# 如需更换本地 embedding 模型，可复制为 .env 并设置：
# EMBEDDING_MODEL_LOCAL=BAAI/bge-small-zh-v1.5

# ===== 语音转文字 / 大模型（阿里云百炼 DashScope）=====
# 语音识别(ASR)、图片识别、对话大模型共用同一个 Key。
# 未配置时：/api/asr 走降级占位转写（可演示）；配置后为真实识别。
# 领取：https://bailian.console.aliyun.com → API-KEY 管理 → 创建（形如 sk-xxxx）
# DASHSCOPE_API_KEY=sk-你的key
# ASR_MODEL=paraformer-realtime-v2

# ===== 用户体系数据库 MySQL（B 负责；A 不依赖，A 开发可忽略本段）=====
# 仅 /api/user/* 使用。复制本文件为 .env 后按本机 MySQL 填写。
# 建库、建表、初始化数据、开发库账号授权见 README「用户体系数据库」一节。
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_USER=cxz_user
# DB_PASSWORD=cxz_user_dev_2026
# DB_NAME=chexiaozhi_user
