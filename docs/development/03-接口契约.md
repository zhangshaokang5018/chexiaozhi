# 03 · 接口契约（单一事实来源）

> 本文档是前后端联调的**唯一契约来源**，字段名、类型均**逐行核对真实代码**（`routes/*.py`、`utils/request.ts`、`agents/*.py`、`knowledge/*.json`）得出。
> 任一方要改字段，**先改本文档**并通知对方。base URL：`http://localhost:5000`，请求/响应均为 JSON（ASR 除外，为 multipart 上传）。

---

## 接口总表

| # | 接口 | 方法 | 归属 | 状态 |
|---|------|------|------|------|
| 1 | `/api/ping` | GET | A | ✅ 已通 |
| 2 | `/api/chat` | POST | A | ⚠️ 已通但**返回占位**（未接智能体） |
| 3 | `/api/asr` | POST(上传) | A | ✅ 已通（含降级） |
| 4 | `/api/kb/<kind>` | GET | A | ✅ 已通 |
| 5 | `/api/kb/<kind>/<item_id>` | GET | A | ✅ 已通 |
| 6 | `/api/context` | GET / POST | A | ✅ 接口已通，前端未接 |
| 7 | `/api/receipt` | POST | A | ✅ 接口已通，前端未接 |
| 8 | `/api/user/login` | POST | B | 🆕 待新增 |
| 9 | `/api/user/profile` | GET / PATCH | B | 🆕 待新增 |
| 10 | `/api/user/privacy` | GET / PATCH | B | 🆕 待新增 |
| 11 | `/api/user/stats` | GET | B | 🆕 待新增 |
| 12 | `/api/user/vehicle` | GET / PATCH | B | 🆕 待新增 |

> 分工更新：表中 1~7 是当前已有主业务接口，后续开发由 A 统一维护和接通；8~12 是新增用户体系接口，由 B 独立实现。B 前端调用建议新建 `utils/user-api.ts`，不要直接改 A 的 `utils/request.ts`，避免两条开发线冲突。

---

## 1. `GET /api/ping` — 服务连通检测

**请求**：无参数。

**响应**：
```json
{ "ok": true, "name": "chexiaozhi-server", "version": "0.1.0" }
```
（根路径 `GET /` 额外返回 `endpoints` 列表。）

---

## 2. `POST /api/chat` — 对话主入口 ⭐

**请求体**：
```json
{
  "user_id": "test_user_001",   // ⚠️ 前端发送, 后端当前未读取(见下方注意)
  "text": "800 换机油机滤合理吗",
  "mode": "text",                // "text" | "image"
  "image_label": ""              // mode=image 时的图片标签, 如 "报价单"
}
```

> **注意（合并断点）**：后端 `routes/chat.py` 当前只读取 `text` / `mode` / `image_label`，**未读取 `user_id`**，因此对话无法关联车辆上下文、也无法沉淀维修记录。打通时需补上 `user_id` 的读取与透传。

**响应体**：
```json
{
  "route": {
    "agent": "maintain",         // scheduler|symptom|dtc|maintain|part
    "intent": "保养建议/报价审核",
    "confidence": 0.9
  },
  "agent": "maintain",
  "agent_meta": { "name": "保养建议 Agent", "desc": "报价/避坑指南" },
  "hit": true,                   // 后端当前规则: agent != "scheduler" 即 true
  "reply": {
    "title": "保养建议 / 报价审核",
    "summary": "……",
    "blocks": [
      { "type": "kv",   "label": "可能原因", "value": "……" },
      { "type": "warn", "text": "维修前建议确认项目明细、配件品牌、工时费和质保条款。" },
      { "type": "danger", "text": "🚨 高风险提醒……" }   // 高危场景
    ],
    "price_text": "参考 800-2500 元",   // 占位阶段为空字符串 ""
    "price_range": [800, 2500]          // 占位阶段为 []
  },
  "steps": [
    { "name": "意图识别", "status": "done", "detail": "保养建议/报价审核（90%）" },
    { "name": "分发至 保养建议 Agent", "status": "done" },
    { "name": "知识库检索", "status": "done" },
    { "name": "合成回复", "status": "done" }
  ],
  "elapsed_ms": 12,
  "legal_note": "所有建议仅供参考，请以当地授权维修点为准。"
}
```

**`blocks[].type` 取值（当前前端类型）**：`kv`（键值对，用 `label`/`value`）｜`warn`（黄色警告，用 `text`）｜`danger`（红色高危，用 `text`）。前端还支持 `good`/`danger` 布尔标记位用于颜色。

**`steps[].status` 取值（当前前端类型）**：`done` | `doing` | `pending` | `todo`。

> ⚠️ 接入风险：`agents/maintain.py` 的真实返回里，`retrieve_node()` 未命中时会把 `steps[2].status` 写成 `miss`，`assess_node()` 报价合理时会输出 `{"type": "good", ...}`。这两个值**当前不在 `utils/request.ts` 的 TypeScript 联合类型里**，也不在上面的 chat 契约里。A-T1 接入 maintain 时必须二选一：要么后端把 `miss/good` 规范化成前端已有值，要么同步扩展前端类型与 WXML 渲染规则。

### 2.1 scheduler 意图路由规则（`agents/scheduler.py`）

| 条件 | agent | confidence | intent |
|------|-------|-----------|--------|
| `mode == "image"` | `part` | 0.95 | 图像识别/零件匹配 |
| 文本或 `image_label` 含 `[PBC]\d{4}` 故障码（正则，忽略大小写） | `dtc` | 0.92 | 故障码解读 |
| 含关键词：报价/费用/价格/多少钱/合理/贵/保养 | `maintain` | 0.9 | 保养建议/报价审核 |
| 含关键词：响/抖/顿挫/报警/灯亮/水温/机油/刹车/转向 | `symptom` | 0.86 | 症状分析 |
| 其它 | `scheduler` | 0.55 | 综合咨询/需补充信息 |

> 优先级以代码实现为准：**图片模式优先**，然后 DTC 正则、报价/保养关键词、症状关键词、兜底。因此 `mode=image` 时即便文本里带 `P0300`，也会先路由到 `part`。

### 2.2 ⚠️ 当前 vs 目标

- **当前**：`reply` 由 `build_reply()` 返回**写死占位文案**，`price_text=""`、`price_range=[]`，不查知识库、不做 RAG。
- **目标**：路由命中后调用对应智能体的 `handle(text, context)`（如 `agents/maintain.handle`），用其真实返回填充 `reply`/`steps`/`price_*`，并把 `receipt_item` 沉淀到 context。详见 [05](./05-任务清单与开发计划.md)。

---

## 3. `POST /api/asr` — 语音转文字

**请求**：`multipart/form-data`，文件字段名 **`file`**。支持 `.mp3 / .wav / .pcm / .aac / .m4a`。前端录音规格：MP3、16000Hz、单声道、≤60s。

**响应**：
```json
{
  "ok": true,
  "text": "发动机怠速时咕噜咕噜响",
  "simulated": false,                  // true=降级占位(未配置 Key 或识别失败)
  "model": "paraformer-realtime-v2",   // 或 "fallback" / "none"
  "error": null
}
```

---

## 4. `GET /api/kb/<kind>` — 知识库查询

**路径参数** `kind`：`dtc` | `cost` | `symptom`
**查询参数**：`q`（关键词）、`level`（如 `high`）、`page`（默认 1）、`page_size`（默认 50，最大 100）

**响应**：
```json
{
  "kind": "dtc",
  "count": 5,
  "total": 5,
  "page": 1,
  "page_size": 50,
  "query": "P0300",
  "items": [
    {
      "id": "P0300",
      "kind": "dtc",
      "title": "P0300",
      "subtitle": "发动机缺火（随机/多缸）",
      "summary": "……",
      "detail": "……",
      "level": 3,
      "level_text": "难度 3/5",       // dtc: 难度 N/5; symptom: 高/中/低; cost: 参考价
      "level_class": "",              // dtc level>=4 或 symptom=高 时为 "danger", 否则 ""
      "stars": "★★★☆☆",
      "price_text": "零件 800-2000 元·工时 200-500 元",
      "tip": "……",
      "ask_text": "P0300 是什么意思",  // 「一键咨询」时发送给 /api/chat 的文本
      "tags": ["发动机", "缺火"],
      "raw": { /* 原始 JSON 字段 */ }
    }
  ],
  "tabs": [
    { "kind": "dtc", "label": "故障码", "count": 5 },
    { "kind": "cost", "label": "维修成本", "count": 5 },
    { "kind": "symptom", "label": "症状关联", "count": 5 }
  ]
}
```

**搜索策略**（`routes/kb.py`）：① `dtc` 类先提取 `[PBC]\d{4}` 精确匹配 → ② RAG 语义检索（距离≤0.55，取前 5）→ ③ JSON 子串匹配兜底。

---

## 5. `GET /api/kb/<kind>/<item_id>` — 知识库单条详情

**响应**：单条规范化 `item`（结构同上 `items[]` 元素）。

---

## 6. `GET / POST /api/context` — 车辆上下文

**`GET /api/context?user_id=<user_id>`** 响应（VIN 脱敏）：
```json
{
  "car_model": "2022款 丰田 卡罗拉 1.2T 豪华版",
  "vin": "LFMA*********3456",        // 脱敏: 前4 + 后4, 中间 *
  "mileage": "38,500 km",
  "location": "北京",
  "receipts": [ /* 已沉淀的诊断项 */ ]
}
```

**`POST /api/context`** 请求（字段均可选，仅传需更新的）：
```json
{ "user_id": "test_user_001", "car_model": "…", "vin": "…", "mileage": "…", "location": "…" }
```
响应：
```json
{
  "ok": true,
  "context": {
    "car_model": "2022款 丰田 卡罗拉 1.2T 豪华版",
    "vin": "LFMA*********3456",
    "mileage": "38,500 km",
    "location": "北京",
    "receipts": []
  }
}
```

> 存储为内存（`_STORE[user_id]`），**服务重启即清空**（MVP）。默认测试用户已内置示例车辆。

---

## 7. `POST /api/receipt` — 维修记录存根

**请求**：`{ "user_id": "test_user_001" }`

**响应**：
```json
{
  "shop": "车智汇授权维修中心",
  "shop_code": "91110108MA01XXXXXXXX",
  "car_model": "……",
  "vin": "LFMA*********3456",
  "location": "北京",
  "created_at": "2026-06-27 10:00:00",
  "items": [
    { "item": "更换机油机滤", "part_range": "300-500", "labor_range": "100-200", "avg": 550 }
  ],
  "total": 550,             // items[].avg 之和; 无诊断时 items=[], total=0
  "simulated": true         // MVP 阶段恒为 true
}
```

---

## 8. 知识库 JSON Schema（`knowledge/*.json`）

### 8.1 `kb_dtc.json` — 故障码（5 条：P0300/P0301/P0171/P0420/P0128）
```json
{
  "code": "P0300", "desc": "发动机缺火（随机/多缸）",
  "plain": "大白话解释", "cause": "可能原因",
  "level": 3,                       // 整数 1-5, 越大越严重
  "logic": "诊断流程",
  "price_part_low": 800, "price_part_high": 2000,
  "price_labor_low": 200, "price_labor_high": 500
}
```

### 8.2 `kb_cost.json` — 维修成本（5 条：机油/刹车片/火花塞/空调滤芯/防冻液）
```json
{
  "cat": "紧凑型轿车", "item": "更换机油机滤",
  "part_low": 300, "part_high": 500,
  "labor_low": 100, "labor_high": 200,
  "tip": "避坑提示"
}
```

### 8.3 `kb_symptom.json` — 症状关联（5 条：异响/油压/刹车/抖动/水温）
```json
{
  "keywords": ["咕噜咕噜", "呼噜声", "异响", "水泵"],
  "fault": "水泵故障 / 冷却液循环异常 / 排气管共振",
  "level": "中",                     // 字符串 高|中|低
  "tip": "诊断建议",
  "part_low": 300, "part_high": 1500,
  "labor_low": 100, "labor_high": 400
}
```

> 注意 `dtc.level` 是整数(1-5)、`symptom.level` 是字符串(高/中/低)，两者不同。

---

## 9. 智能体返回契约（供 chat.py 接入参考）

`agents/maintain.handle(text, context) -> dict` 真实返回结构（**这是 chat.py 接入时要消费的对象**）：
```json
{
  "agent": "maintain",
  "agent_meta": { "name": "保养建议 Agent", "desc": "报价/避坑指南" },
  "intent": "保养建议/报价审核",
  "confidence": 0.9,                 // 命中 0.9 / 未命中 0.5
  "hit": true,
  "reply": { "title": "...", "summary": "...", "blocks": [...], "price_text": "...", "price_range": [low, high] },
  "steps": [ { "name": "...", "status": "done|miss", "detail": "..." } ],
  "receipt_item": {                  // 仅 hit=true 时存在, 供 context.add_receipt_item 沉淀
    "item": "更换机油机滤", "part_range": "300-500", "labor_range": "100-200", "avg": 550
  }
}
```

> 接入约定：`/api/chat` 拿到智能体返回后，用其 `reply/steps/hit/confidence` 覆盖占位，并把 `route` 外层包好（保持本文档第 2 节的响应外形不变）。但不能假设"前端零改动"一定成立：接入 `maintain` 前必须处理 `miss/good` 这两个当前前端类型未覆盖的值。dtc/symptom/part 智能体建成后应保持**相同的 `handle()` 返回契约**，并优先使用前端已支持的 block/status 枚举。

---

## 10. B 用户体系接口（本期任务）

> 下面接口已从“后续备查”调整为 **B 用户体系本期任务**。字段是开发前约定，代码实现前如需变更，先改本文档。
> **存储**：8~12（`/api/user/*`）由 B 落地到本地 **MySQL** 库 `chexiaozhi_user`，表结构见下方 §11；接口字段与表字段对应。A 的接口（1~7）不使用数据库。

### 10.1 `POST /api/user/login` — 登录/创建用户

**请求体**：
```json
{
  "login_type": "wechat",          // "wechat" | "guest"
  "code": "wx-login-code",         // wechat 登录时传; guest 可为空
  "anonymous_id": "local-device-id" // guest 登录时传; 可由前端本地生成
}
```

**响应体**：
```json
{
  "ok": true,
  "user_id": "user_10001",
  "session_token": "dev-session-token",
  "is_new_user": false,
  "profile_completed": true
}
```

MVP 可先做模拟登录，但必须返回稳定 `user_id`。A 的 `/api/chat`、`/api/context`、`/api/receipt` 只认 `user_id`，不依赖 `session_token`。

### 10.2 `GET /api/user/profile` — 获取用户资料

**请求参数**：`user_id`

**响应体**：
```json
{
  "user_id": "user_10001",
  "nickname": "车小智用户",
  "avatar_url": "",
  "phone_masked": "138****8000",
  "created_at": "2026-06-27 10:00:00",
  "profile_completed": true
}
```

### 10.3 `PATCH /api/user/profile` — 更新用户资料

**请求体**：
```json
{
  "user_id": "user_10001",
  "nickname": "新的昵称",
  "avatar_url": "https://example.com/avatar.png"
}
```

**响应体**：同 `GET /api/user/profile`。

### 10.4 `GET / PATCH /api/user/privacy` — 隐私与私人信息设置

**GET 请求参数**：`user_id`

**响应体**：
```json
{
  "user_id": "user_10001",
  "phone_authorized": false,
  "show_vin": false,
  "share_diagnosis_for_improvement": false,
  "data_retention_days": 180
}
```

**PATCH 请求体**：
```json
{
  "user_id": "user_10001",
  "show_vin": false,
  "share_diagnosis_for_improvement": false,
  "data_retention_days": 180
}
```

隐私原则：手机号、VIN、登录 token 不返回明文；前端展示手机号用 `phone_masked`，车辆 VIN 展示遵守 `show_vin`，默认脱敏。

### 10.5 `GET /api/user/stats` — 我的页统计

**请求参数**：`user_id`

**响应体**：
```json
{
  "consult_count": 12,
  "receipt_count": 3,
  "estimated_saved": 680,
  "favorite_count": 0
}
```

MVP 阶段数据来自 MySQL 的 `user_stats` 表，可先写入演示/模拟统计值，但不要再使用进程内内存作为正式来源。字段必须稳定，供 `pages/profile` 渲染。

### 10.6 `GET / PATCH /api/user/vehicle` — 用户车辆资料

**GET 请求参数**：`user_id`

**响应体**：
```json
{
  "user_id": "user_10001",
  "car_model": "2022款 丰田 卡罗拉 1.2T 豪华版",
  "vin": "LFMA*********3456",
  "mileage": "38,500 km",
  "location": "北京"
}
```

**PATCH 请求体**：
```json
{
  "user_id": "user_10001",
  "car_model": "2022款 丰田 卡罗拉 1.2T 豪华版",
  "vin": "LFMAP22CXXX123456",
  "mileage": "39,000 km",
  "location": "北京"
}
```

与 A 的 `/api/context` 的关系：`/api/user/vehicle` 是 B 的用户资料视角，`/api/context` 是 A 的诊断上下文视角。MVP 可先保持字段一致但存储分离；集成时由 A/B 共同决定是否让 `/api/context` 读取 B 的用户车辆资料。不要在两个分支里同时改同一个服务文件来做这件事。

### 10.7 后续接口（本期不强制）

| 功能 | 建议接口 | 备注 |
|------|----------|------|
| 维修记录列表 | `GET /api/user/repairs?user_id=` | B 做我的页列表入口；A 维护 receipt 详情 |
| 收藏知识库 | `GET/POST /api/user/favorites` | 后续 |
| 附近维修点 | `GET /api/shops/nearby` | 后续 |

---

## 11. 用户体系数据库 Schema（MySQL，B 负责）

> 库名 `chexiaozhi_user`（utf8mb4）。建库建表脚本：`car-server/sql/user_schema.sql`。仅 B 的 `/api/user/*` 使用；A 不依赖。字段如需变更，先改本文档与该 SQL 脚本。

| 表 | 主键 | 关键字段 | 对应接口 |
|----|------|----------|----------|
| `users` | `user_id` (VARCHAR) | `login_type`、`openid`、`session_token`、`nickname`、`avatar_url`、`phone`(脱敏返回)、`profile_completed`、`created_at` | `/api/user/login`、`/api/user/profile` |
| `user_privacy` | `user_id` | `phone_authorized`、`show_vin`、`share_diagnosis_for_improvement`、`data_retention_days` | `/api/user/privacy` |
| `user_vehicle` | `user_id` | `car_model`、`vin`(脱敏)、`mileage`、`location` | `/api/user/vehicle` |
| `user_stats` | `user_id` | `consult_count`、`receipt_count`、`estimated_saved`、`favorite_count` | `/api/user/stats` |

约定：
- `user_id` 为对外稳定主键（如 `user_10001`），由 B 在登录时生成；A 的 chat/context/receipt 只认这个 `user_id`。
- `users.phone`、`user_vehicle.vin` 明文落库，但接口对外只返回脱敏值（`phone_masked` / VIN 前4后4），且默认不展示完整值。
- `session_token` 只允许在 `POST /api/user/login` 成功响应里返回一次，供前端写入 `cxz_session_token`；其它接口不得返回 token 明文，也不得写入日志。
- `user_privacy`/`user_vehicle`/`user_stats` 通过外键 `ON DELETE CASCADE` 关联 `users`，便于退出/注销时级联清理。
- `user_stats` 的计数 MVP 阶段由 B 维护；若要反映 A 的真实诊断/receipt 数据，先在本文档约定新接口，再单独集成，不直接改 A 的 `services/context.py`。
- 本期只建上述 4 张表。`GET /api/user/repairs` 属于 B-U7 的入口/列表体验，MVP 可返回空态或模拟列表；若后续要真实维修记录列表，再新增 `user_repairs` 表并同步更新本文档与 `sql/user_schema.sql`。

### 11.1 初始化数据（B 本地联调必备）

`car-server/sql/user_schema.sql` 不只建库建表，也会创建一组**演示用户数据**，方便 B 在接口未完全写好前先验证 MySQL 读写。

执行：
```bash
cd car-server
mysql -u root -p < sql/user_schema.sql
mysql -u root -p -e "CREATE USER IF NOT EXISTS 'cxz_user'@'localhost' IDENTIFIED BY 'cxz_user_dev_2026'; CREATE USER IF NOT EXISTS 'cxz_user'@'127.0.0.1' IDENTIFIED BY 'cxz_user_dev_2026'; GRANT SELECT, INSERT, UPDATE, DELETE ON chexiaozhi_user.* TO 'cxz_user'@'localhost'; GRANT SELECT, INSERT, UPDATE, DELETE ON chexiaozhi_user.* TO 'cxz_user'@'127.0.0.1'; FLUSH PRIVILEGES;"
```

脚本会插入或更新以下演示数据（重复执行安全，使用 `ON DUPLICATE KEY UPDATE`）：

| 表 | 初始化数据 |
|----|------------|
| `users` | `user_id=user_10001`，`login_type=wechat`，`nickname=车小智用户`，`phone=13800008000`，`profile_completed=1` |
| `user_privacy` | `user_id=user_10001`，使用默认隐私设置：手机号未授权、VIN 不展示完整值、数据保留 180 天 |
| `user_vehicle` | `user_id=user_10001`，车型 `2022款 丰田 卡罗拉 1.2T 豪华版`，VIN `LFMAP22CXXX123456`，里程 `38,500 km`，位置 `北京` |
| `user_stats` | `user_id=user_10001`，`consult_count=12`，`receipt_count=3`，`estimated_saved=680` |

验证初始化数据：
```bash
mysql -u root -p -e "USE chexiaozhi_user; SELECT user_id,nickname,phone,profile_completed FROM users WHERE user_id='user_10001';"
mysql -u root -p -e "USE chexiaozhi_user; SELECT * FROM user_vehicle WHERE user_id='user_10001';"
mysql -u root -p -e "USE chexiaozhi_user; SELECT * FROM user_stats WHERE user_id='user_10001';"
mysql -h 127.0.0.1 -u cxz_user -pcxz_user_dev_2026 -e "USE chexiaozhi_user; SELECT user_id,nickname FROM users WHERE user_id='user_10001';"
```

账号与密码说明：

| 类型 | 账号/标识 | 密码 | 用途 |
|------|-----------|------|------|
| MySQL root | `root` | 本机安装 MySQL 时设置的 root 密码 | 只用于建库、建表、授权，不写入 `.env` |
| B 本地开发库账号 | `cxz_user` | `cxz_user_dev_2026` | `/api/user/*` 连接 `chexiaozhi_user` 使用，写入 `.env` |
| 小程序演示业务用户 | `user_10001` | 无业务密码 | 初始化数据中的演示用户；当前 MVP 是微信/游客登录，`/api/user/login` 成功后返回 `session_token` |

`.env` 推荐值：
```ini
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=cxz_user
DB_PASSWORD=cxz_user_dev_2026
DB_NAME=chexiaozhi_user
```

B 开发 `/api/user/login` 时，可以先让接口读取 `user_10001` 返回，以验证 `routes/user.py → services/user.py → services/db.py → MySQL` 链路。后续真正登录流程完成后，再由登录逻辑创建新用户，不再依赖这条演示数据。
