# 06 · 并行开发边界与合并规则

> 本文档专门解决 A、B 分别拉代码开发时的冲突问题。原则是：A 负责现有主业务闭环，B 负责新增用户体系；两人只通过稳定契约交互，不在同一批文件里互相改逻辑。

---

## 1. 总原则

1. **A 主业务线优先保持可运行**：首页、咨询、知识库、智能体、RAG、上下文、维修存根都归 A。
2. **B 用户体系独立开发**：登录、我的页、用户资料、私人信息、隐私设置、用户统计都归 B。
3. **共享文件只做注册，不写业务**：`app.py`、`app.json`、`utils/request.ts` 属于高冲突文件，除非本文件明确允许，否则不要在各自分支里随意改。
4. **交界只认 `user_id`**：A 不处理手机号、登录 token、隐私开关等私人信息；B 登录后提供稳定 `user_id`，A 用它关联 context/chat/receipt。

---

## 2. 已提前处理的小冲突点

### 2.1 后端 `app.py`

已做小改动：`app.py` 会用 `importlib.util.find_spec("routes.user")` 检测是否存在 `routes/user.py`，存在则 `from routes.user import user_bp` 并自动注册；不存在则现有服务不受影响。

**结果**：B 开发 `/api/user/*` 时只需要新增 `routes/user.py`，不需要再改 `app.py`。

> ⚠️ **硬约定（B 必须遵守）**：
> 1. `routes/user.py` **必须**导出一个**模块级变量，名字精确为 `user_bp`**，且为 `Blueprint` 实例：
>    ```python
>    from flask import Blueprint
>    user_bp = Blueprint("user", __name__)
>    ```
> 2. `app.py` 现已把这行导入包进 `try/except`：当 `routes/user.py` 存在但导入失败（语法错误、未导出 `user_bp` 等）时，**降级为「不注册 user 蓝图」并打印告警，A 的主业务接口照常启动**，不会被一起带崩。即便如此，B 新增 `routes/user.py` 后仍应**第一时间确认 `python app.py` 能正常启动且 `/api/user/*` 已注册**，避免自己的接口悄悄没生效。

### 2.2 前端 `app.json`

暂不提前注册 `pages/login`、`pages/privacy`。原因是小程序页面路径写入 `app.json` 后，若页面文件不存在，会影响编译。

**规则**：B 新增登录/隐私页面时，由 B 一次性新增页面文件并修改 `app.json`。如果 A 同期正在做导航统一，则先合 A 的导航，再由 B rebase 后补页面路径。

> A-T9（导航/tabBar 统一）是 P3 可延后项，且同样会改 `app.json`。为降低与 B 的页面注册冲突，**建议 A-T9 排在 B 的 `app.json` 页面注册合并之后**再做。

### 2.3 用户数据存储 `car-server/data/`（PII，必须忽略）

B 的用户体系如果有本地导出/缓存数据（`car-server/data/users.*` 等），可能包含手机号、openid、session token 等**敏感信息（PII）**。这类文件必须忽略，否则会有两个风险：

1. **隐私泄漏**：用户 PII 被误提交进 git 历史（与 §6「不把手机号/token 明文入库」原则冲突）。
2. **共享冲突**：`data/users.json` 作为运行时可变文件被提交后，A、B、不同环境每次运行都会改它，造成无意义的 diff 与合并冲突。

**已落地**：`car-server/.gitignore` 已加入：

```gitignore
# 用户体系本地数据（PII，不入库；格式由 B 选择）。需样例时单独提供 data/users.example.json
data/
```

> 若 B 需要保留一份"样例数据"供他人跑通，应单独提供 `data/users.example.json`（不含真实 PII）并用 `git add -f` 显式纳入版本控制。

### 2.4 用户体系数据库 MySQL（B 的存储；依赖/配置已隔离，A 不受影响）

B 用户体系的存储选型为 **MySQL（本地）**，库名 `chexiaozhi_user`。为保证「A 不被影响、A/B 提交不冲突」，已做如下隔离与预埋：

| 关注点 | 处理 | 谁负责 |
|--------|------|--------|
| **依赖** | MySQL 依赖只放 B 独占的 `requirements-user.txt`（PyMySQL、python-dotenv），**不写进共享的 `requirements.txt`**。A 不装也能跑全部主业务。 | 已建好 `requirements-user.txt` |
| **连接配置** | `.env.example` 已预留 `DB_HOST/DB_PORT/DB_USER/DB_PASSWORD/DB_NAME`（注释态）。B 复制为 `.env` 后使用 `DB_USER=cxz_user`、`DB_PASSWORD=cxz_user_dev_2026`、`DB_NAME=chexiaozhi_user`。root 只用于本地建库/授权。 | 已预埋 |
| **建库建表与初始化数据** | `car-server/sql/user_schema.sql` 已提供（建库 + 4 张表 + 演示账号/隐私/车辆/统计）。演示业务用户为 `user_10001`，当前 MVP 无业务密码，登录成功后由 B 返回 `session_token`。 | 已提供 |
| **启动隔离** | **硬约定：B 的 `services/db.py` 必须惰性连库（首次查询才连接），不能在模块导入时连库。** 否则 MySQL 没起时会随 `app.py` 导入 `user_bp` 一起报错。§2.1 已容错降级为「不注册 user 蓝图」，A 不会被带崩；但 B 自己的接口会整段失效。惰性连接下：MySQL 没起也不影响 A，且 `/api/user/*` 被调用时能返回可恢复数据库错误。 | B |
| **数据隔离** | 用户数据全在 `chexiaozhi_user` 库；A 的内存上下文 / Chroma / knowledge JSON 与之零交集。 | — |

> 一句话：A 完全不碰 MySQL（不装依赖、不配 `DB_*`、代码不连库）；B 的库挂了也只影响 `/api/user/*`。建库步骤见 `car-server/README.md`「用户体系数据库」。

---

## 3. 文件归属

### A 独占

| 路径 | 说明 |
|------|------|
| `car-server/routes/chat.py` | 对话主入口 |
| `car-server/routes/kb.py` | 知识库接口 |
| `car-server/routes/context.py` | 诊断上下文 |
| `car-server/routes/receipt.py` | 维修存根 |
| `car-server/routes/asr.py` | 语音转文字 |
| `car-server/agents/*` | scheduler、maintain、dtc、symptom、part |
| `car-server/services/rag.py` | RAG 检索 |
| `car-server/services/context.py` | 诊断上下文存储 |
| `car-server/services/receipt.py` | 维修存根生成 |
| `car-server/knowledge/*.json` | 知识库数据 |
| `car-miniapp/miniprogram/pages/index` | 首页 |
| `car-miniapp/miniprogram/pages/chat` | 咨询页 |
| `car-miniapp/miniprogram/pages/kb` | 知识库页 |
| `car-miniapp/miniprogram/pages/detail` | 故障码详情 |
| `car-miniapp/miniprogram/pages/receipt` | 维修存根页 |
| `car-miniapp/miniprogram/utils/request.ts` | 主业务 API 类型 |

### B 独占

| 路径 | 说明 |
|------|------|
| `car-server/routes/user.py` | 用户接口 |
| `car-server/services/user.py` | 用户登录、资料、隐私、统计（读写 MySQL） |
| `car-server/services/db.py` | MySQL 连接层（PyMySQL，惰性连接） |
| `car-server/services/user_init.py` | 可选：Python 侧建库建表与初始化数据脚本 |
| `car-server/sql/*.sql` | 建库建表与初始化数据 DDL（`user_schema.sql` 已提供） |
| `car-server/requirements-user.txt` | B 用户体系依赖（PyMySQL、python-dotenv） |
| `car-miniapp/miniprogram/pages/profile` | 我的页 |
| `car-miniapp/miniprogram/pages/login` | 登录页 |
| `car-miniapp/miniprogram/pages/privacy` | 隐私设置页 |
| `car-miniapp/miniprogram/utils/user-api.ts` | 用户体系 API 类型 |
| `car-server/data/*` | 用户本地导出/缓存（如有），必须 gitignore（见 §2.3） |

### 共享 / 默认模板（避免无主冲突）

| 路径 | 规则 |
|------|------|
| `car-miniapp/miniprogram/utils/util.ts` | 小程序模板自带工具（`formatTime` 等）。默认归 A 维护；A/B 都尽量**只读不改**，确需新增公共方法时各自放到自己的 `request.ts` / `user-api.ts`，不要往这里堆。 |
| `car-server/services/__init__.py`、`routes/__init__.py`、`agents/__init__.py` | 包初始化文件，通常无需改动；如需改动先在群里同步。 |

### 共享但受控

| 路径 | 规则 |
|------|------|
| `car-server/app.py` | 已预留 `user_bp` 自动注册；B 不再改 |
| `car-miniapp/miniprogram/app.json` | B 可新增 `pages/login/login`、`pages/privacy/privacy`；A 做 tabBar/导航时需提前沟通 |
| `car-miniapp/miniprogram/app.ts` | 只允许写全局登录态初始化；能放到 `utils/user-api.ts` 就不要改 |

---

## 4. `user_id` 统一规则

| 项 | 约定 |
|----|------|
| storage key | `cxz_user_id` |
| session key | `cxz_session_token` |
| 游客默认 | 未登录时可降级为 `guest_user_001` 或后端返回的 guest user |
| A 读取方式 | A-T5 后从 storage 读取 `cxz_user_id`，没有则用测试/游客值 |
| B 写入方式 | B-U1 登录成功后写入 `cxz_user_id` 和 `cxz_session_token` |

`user_id` 是 A/B 唯一强交界。B 不要求 A 消费手机号、openid、session token；A 不要求 B 直接写诊断记录。`session_token` 只允许在登录成功响应里出现一次，之后由前端存储在 `cxz_session_token`，其它接口不得返回 token 明文。

---

## 5. 合并顺序建议

1. A 先合 `A-T1` + `A-T2`，让 `/api/chat` 接上 maintain。
2. B 可并行开发 `B-U1` + `B-U2`，但不要改 A 独占文件。
3. 如果 B 需要新增页面路径，等页面文件齐全后再改 `app.json`。
4. A-T5 与 B-U1 都涉及 `user_id`，以本文档 storage key 为准，不互相改对方代码。
5. 两条线都合入后，再单独开一个小集成任务决定 `/api/context` 是否读取 B 的 `/api/user/vehicle` 数据源。

---

## 6. 不允许的做法

- B 不直接改 `pages/chat` 来读取登录态；由 A-T5 处理。
- B 不直接改 `services/context.py` 来同步车辆资料；先做 `services/user.py`。
- A 不直接改 `pages/profile` 做用户展示；用户页归 B。
- A/B 都不要在没有同步文档的情况下改接口字段。
- 不把登录 token、手机号、VIN 明文写进日志或普通响应。
